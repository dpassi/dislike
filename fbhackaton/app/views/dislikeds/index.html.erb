<p id="notice"><%= notice %></p>

<h1>Dislikes</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>quantity</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @dislikes.each do |disliked| %>
      <tr>
        <td><%= disliked.name %></td>
        <td><%= disliked.quantity %></td>
        <td>
          <button name="button" type="button">
          <strong>DISLIKE</strong>
          </button>

          <form method="PUT" action="/dislikeds/1" class="button_to">
                <input value="DISLIKE" type="submit" />
          </form>

          




        </td>
    <!--    <td><%= link_to 'Show', disliked %></td>
        <td><%= link_to 'Edit', edit_disliked_path(disliked) %></td>
        <td><%= link_to 'Destroy', disliked, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
      </tr>
    <% end %>
  </tbody>
</table>

<br>
